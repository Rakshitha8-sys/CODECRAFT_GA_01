# CODECRAFT_GA_01
This project demonstrates text generation using the GPT-2 (Generative Pre-trained Transformer 2) model. The model is fine-tuned on a custom dataset related to Machine Learning concepts and is capable of generating meaningful and context-aware text based on a given input prompt.
